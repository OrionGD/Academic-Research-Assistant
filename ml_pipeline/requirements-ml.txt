# ARAS ML Pipeline - Minimal Requirements by Use Case

# ========== BASE CORE ==========
# Required for all setups
numpy>=1.24.0
scipy>=1.10.0
python-dotenv>=1.0.0
pydantic>=2.5.0
tenacity>=8.2.0
aiohttp>=3.9.0
tqdm>=4.65.0
nltk>=3.8.1

# ========== EMBEDDINGS (CHOOSE ONE) ==========

# Option A: gemini Embeddings (cloud, paid)
gemini>=1.6.0
tiktoken>=0.5.0

# Option B: Local Sentence Transformers (free, self-hosted)
sentence-transformers>=2.2.2
torch>=2.0.0

# Option C: Google Embeddings
google-generativeai>=0.3.0

# ========== VECTOR DATABASE (CHOOSE ONE) ==========

# Option A: MongoDB Atlas (with vector search)
pymongo>=4.5.0

# Option B: ChromaDB (local)
chromadb>=0.4.0

# Option C: Qdrant (local/cloud)
qdrant-client>=1.7.0

# ========== LLM (CHOOSE ONE) ==========

# Option A: gemini
gemini>=1.6.0

# Option B: Anthropic
anthropic>=0.18.0

# Option C: Google
google-generativeai>=0.3.0

# Option D: Local (Ollama)
ollama>=0.1.0

# Option E: Local (llama.cpp)
# llama-cpp-python>=0.2.0

# ========== DOCUMENT PROCESSING (OPTIONAL) ==========

# PDFs
pypdf>=3.17.0

# Word documents
python-docx>=0.8.11

# Markdown
markdown>=3.5.0

# HTML
beautifulsoup4>=4.12.0

# Excel
openpyxl>=3.1.0
